/**
 * Chain access for the web surfaces: a viem public client plus deployed
 * contract addresses loaded from the deployment artifacts JSON. Read-only —
 * transaction submission is not this module's job.
 */

export {};
