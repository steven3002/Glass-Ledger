/**
 * QR utilities: rendering tag QR codes (qrcode.react) and camera scanning
 * (@zxing/browser). Wrappers only — what a scanned payload means is decided
 * by the verification module, never in here.
 */

export {};
